# ===========================================
# Copy to .env.local and fill in your values.
# For Vercel: add these in Project → Settings → Environment Variables
# ===========================================

# --- REQUIRED for login & database ---
# PostgreSQL connection string (Supabase: Project Settings → Database → Connection string URI)
# Use "Connection pooling" URL (port 6543) for serverless/Vercel
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/database?pgbouncer=true"

# Secret for signing session cookies (use a long random string in production)
JWT_SECRET="your-secret-key-change-this-in-production"

# --- REQUIRED for Production (Vercel) ---
# Your live app URL (e.g. https://ogilvy-ai.vercel.app)
NEXT_PUBLIC_BASE_URL="https://your-app.vercel.app"

# --- OPTIONAL: AI schedule (Production Schedule Maker) ---
OPENAI_API_KEY="sk-..."

# --- OPTIONAL: Email (Drowning, requests, etc.) ---
GMAIL_USER="your@gmail.com"
GMAIL_APP_PASSWORD="your-app-password"
ADMIN_EMAIL="admin@example.com"
